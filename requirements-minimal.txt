# EpiGABM minimum to run ABM + GABM + SEIR + hybrid (no viz, no calibration).
# Use this for CI, testing, or a lean experiment-only install.
#     pip install -r requirements-minimal.txt

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

loguru>=0.7
pyyaml>=6.0
pydantic>=2.0
tqdm>=4.65

ollama>=0.1
openai>=1.0
tiktoken>=0.5
tenacity>=8.2

pytest>=7.4
