# EpiGABM full runtime dependencies.
# Python 3.11 or 3.12 required (hmmlearn has no prebuilt wheels for 3.13+ on Windows).
# For a fresh install:
#     py -3.12 -m venv .venv
#     .venv\Scripts\activate
#     pip install -r requirements.txt
# Optional extras live in pyproject.toml under [project.optional-dependencies].
# Full install with everything: pip install -e ".[all]"
# Poetry users: poetry install --all-extras

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
loguru>=0.7
pyyaml>=6.0
pydantic>=2.0
tqdm>=4.65
openai>=1.0
tiktoken>=0.5
ollama>=0.1
tenacity>=8.2
hmmlearn>=0.3
SALib>=1.4
joblib>=1.3
tensorflow>=2.15
streamlit>=1.30
folium>=0.15
plotly>=5.18
geopandas>=0.14
osmnx>=1.9
pytest>=7.4
ruff>=0.1
